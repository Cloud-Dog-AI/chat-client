# cloud-dog-chat-client — requirements.lock (W28A-861-R3 publication prep)
#
# Reproducibility lock for the publishable build.
#
# RE-SEALED 2026-06-08 from a GREEN clean-room build of Dockerfile.public against the
# main/local INTERNAL package index (https://pypi.cloud-dog.net/simple/), which carries
# the cloud-dog platform packages. Pins are the EXACT versions resolved by that build
# (pip freeze of the built image's runtime site-packages). PS-97 §3.3 single-index, no
# --extra-index-url. The 861-R3 boundary is main/local; public pypi.org is downstream
# (W28A-862/863).
#
# Pins are INDEX-AGNOSTIC: bare `name==version`, no host, no credentials. They resolve
# from whichever single index the build environment supplies (internal today; the
# GitHub/public boundary later, once these versions are published there).
#
# CONSISTENCY CHECK (must hold; see EXTERNAL-BUILD.md "Lockfile check"):
#   every direct dependency in pyproject.toml [project].dependencies appears below
#   at a version satisfying its specifier. Verify with:
#     python3 -c "import tomllib,re; d=tomllib.load(open('pyproject.toml','rb'))['project']['dependencies']; \
#       L=open('requirements.lock').read().lower(); \
#       miss=[x for x in d if re.split(r'[<>=!~ ]', x.replace('_','-').lower())[0] not in L]; \
#       print('MISSING:', miss) if miss else print('LOCK OK: all direct deps present')"
#
# ── Cloud-Dog platform packages ──────────────────────────────────
# Resolved from the internal index at the versions below. pyproject.toml uses floor
# (>=) specifiers for most of these; the internal index carried newer point releases,
# which this lock now pins for reproducibility. At the GitHub/public boundary they
# resolve from the active boundary index once published; pins are unchanged (host-free).
cloud-dog-agent==0.1.0
cloud-dog-api-kit==0.13.0
cloud-dog-cache==0.2.0
cloud-dog-config==0.3.2
cloud-dog-db==0.3.0
cloud-dog-idam==0.5.2
cloud-dog-jobs==0.4.1
cloud-dog-llm==0.3.1
cloud-dog-logging==0.4.0
cloud-dog-storage==0.1.6

# ── Third-party transitive closure (resolved from the internal index) ──
# Full closure as actually installed in the GREEN build (includes transitive deps
# pulled in by the platform packages: alembic/sqlalchemy/greenlet, argon2/cffi/
# cryptography/pyjwt/pyotp, jinja2/mako/markupsafe, redis, etc.).
alembic==1.18.4
annotated-types==0.7.0
anyio==4.13.0
argon2-cffi==25.1.0
argon2-cffi-bindings==25.1.0
certifi==2026.5.20
cffi==2.0.0
charset-normalizer==3.4.7
click==8.1.7
cryptography==48.0.0
fastapi==0.115.12
greenlet==3.5.1
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.27.2
hvac==2.3.0
idna==3.18
jinja2==3.1.6
mako==1.3.12
markdown-it-py==4.2.0
markupsafe==3.0.3
mdurl==0.1.2
pycparser==3.0
pydantic==2.12.3
pydantic-core==2.41.4
pydantic-settings==2.11.0
pygments==2.20.0
pyjwt==2.13.0
pyotp==2.9.0
python-dotenv==1.2.2
python-multipart==0.0.20
pyyaml==6.0.1
redis==8.0.0
requests==2.34.2
rich==13.9.4
shellingham==1.5.4
sniffio==1.3.1
sqlalchemy==2.0.50
starlette==0.46.2
typer==0.12.5
typing-extensions==4.15.0
typing-inspection==0.4.2
urllib3==2.7.0
uvicorn[standard]==0.38.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==16.0
